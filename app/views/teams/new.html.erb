<div class="team-new">
  <div class="team-new-container container mt-1">
    <h1>Escolha o nome do seu Novo Time</h1>

    <%= simple_form_for @team do |f| %>
      <div class="card">
        <div class="team-new-card-body card-body">
          <%= f.input :name, label: "Nome do Time", class:"team-new-placeholder", placeholder: "Digite o nome do seu time" %>
          
          <div data-controller="create-team-alert"></div>

          <div class="d-flex justify-content-between">
            <%= link_to "Cancelar", teams_path, class: "btn btn-secondary" %>
            <%= f.submit "Criar Time", class: "btn btn-success" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>